# coinops.yml template for static sites
# Ops scripts only read coinops.yml — they never parse .env or other files.

slug: mysite
domain: mysite.coinflipper.org
type: static

# Directory within the repo to serve
root: public

# For auto-deploy — use "true" for no-op, or a build command if needed
deploy_cmd: "true"
